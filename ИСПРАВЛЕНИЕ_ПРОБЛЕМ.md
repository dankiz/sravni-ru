# 🔧 ИСПРАВЛЕНИЕ ПРОБЛЕМ

## Проблема 1: Ошибка при отправке курса на модерацию

### Возможные причины:
1. ❌ Категория не обязательна - ошибка не из-за этого
2. ❌ Проблема с базой данных
3. ❌ Ошибка при загрузке изображения
4. ❌ Проблема с валидацией данных

### Решение:

**Проверьте консоль браузера (F12)** - там будет детальная информация об ошибке.

Также проверьте терминал где запущен `npm run dev` - там будут логи сервера.

### Что исправлено:
✅ Добавлено детальное логирование ошибок
✅ Категория теперь полностью опциональна
✅ Улучшена обработка ошибок в форме
✅ Валидация categoryId перед сохранением

---

## Проблема 2: Не могу войти в админку

### Логин и пароль не работают

Это означает, что **администратор не был создан в базе данных**.

### ✅ РЕШЕНИЕ:

Выполните команду для создания администратора:

```bash
npm run init-admin
```

Эта команда:
1. Читает `ADMIN_EMAIL` и `ADMIN_PASSWORD` из `.env.local`
2. Создает или обновляет администратора в базе данных
3. Хеширует пароль с помощью bcrypt

### 📋 Проверьте файл `.env.local`:

Убедитесь, что файл `.env.local` существует и содержит:

```env
DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/sravni_ru?schema=public"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="local-dev-key-12345"
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="admin123"
```

### 🔄 Если пароль все еще не работает:

1. **Проверьте что скрипт выполнился успешно:**
   ```bash
   npm run init-admin
   ```
   Должно вывести: `Admin created/updated: admin@example.com`

2. **Проверьте что база данных работает:**
   ```bash
   npx prisma db push
   ```

3. **Проверьте таблицу Admin в базе:**
   ```bash
   npx prisma studio
   ```
   Откройте таблицу `Admin` и проверьте есть ли там запись с email `admin@example.com`

4. **Если нужно изменить пароль:**
   - Измените `ADMIN_PASSWORD` в `.env.local`
   - Запустите `npm run init-admin` снова
   - Перезапустите сервер: `npm run dev`

---

## 🚀 ПОЛНАЯ ПЕРЕУСТАНОВКА (если ничего не помогает):

```bash
# 1. Остановите сервер (Ctrl+C)

# 2. Пересоздайте базу данных
npx prisma db push --force-reset

# 3. Создайте администратора
npm run init-admin

# 4. Запустите сервер
npm run dev
```

⚠️ **Внимание:** `--force-reset` удалит все данные из базы!

---

## 📞 Дополнительная диагностика:

Если проблема сохраняется, проверьте:

1. **Логи сервера** в терминале где запущен `npm run dev`
2. **Консоль браузера** (F12 → Console)
3. **База данных подключена?** Проверьте `DATABASE_URL` в `.env.local`
4. **PostgreSQL запущен?** Убедитесь что сервис PostgreSQL работает






